.venv/
__pycache__/
*.pyc
data/reports/*.json
.pytest_cache/
.claude/

# Documents personnels/reels deposes localement pour des tests manuels
# (jamais de vrais documents d'identite dans ce depot public)
data/samples/*.jpg
data/samples/*.jpeg

# Profils candidats et resultats de quiz (donnees personnelles locales)
data/quiz_results/

# Jeu d'evaluation a grande echelle (1000 dossiers, ~150 Mo d'images
# synthetiques) : trop volumineux pour ce depot public, regenerable localement
# via tests/generate_dataset_images.py. Le manifest (verite terrain, texte pur)
# reste versionne.
data/samples_dataset/*/*.png
